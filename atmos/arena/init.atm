val Shop   = require "arena.shop"
val Battle = require "arena.battle"

task () {
    var ok = await Shop()
    loop {
        while(ok)
        await Transition("level 1/X", [x=0.9,y=0.9], Battle)
        set ok = await Transition("..", [x=0.5,y=0.5], Shop)
    }
}
