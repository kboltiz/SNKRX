require "atmos.env.pico"
set pico = require "pico"

require "menu"

loop {
    pico.push()
    val id = MainMenu()
    pico.pop() ;; cleans up menu buttons on transitions

    match id {
        :arena => await Arena()
        :quit  => return()
    }
}
