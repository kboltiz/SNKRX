require "atmos.env.pico"
set pico = require "pico"

require "shared"
require "settings"

val Menu = require "menu"
val Arena = require "arena"

pico.set.window [ title="SNKRX Menu" ]
pico.set.dim [ '!', w=W, h=H ]

loop {
    val id = await Menu()
    match id {
        :arena => await Transition(Arena)
        :quit  => break()
    }
}
